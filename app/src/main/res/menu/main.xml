<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/test_menu"
        android:title="Test Notification"
        android:icon="@drawable/ic_mms_white_24dp"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/launch_menu"
        android:title="Launch webapp"
        android:icon="@drawable/ic_launch_white_24dp"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/clear_menu"
        android:title="Clear History"
        android:icon="@drawable/ic_change_history_white_24dp"
        app:showAsAction="never"/>

    <item
        android:id="@+id/sign_out_menu"
        android:title="Sign Out"
        android:icon="@drawable/ic_exit_to_app_white_24dp"
        app:showAsAction="never"/>
</menu>